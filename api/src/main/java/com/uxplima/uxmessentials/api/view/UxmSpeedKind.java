package com.uxplima.uxmessentials.api.view;

/** Which of a player's two movement speeds was set. */
public enum UxmSpeedKind {

    /** Walking speed, as set by {@code /walkspeed}. */
    WALK,

    /** Flight speed, as set by {@code /flyspeed}. */
    FLY
}
