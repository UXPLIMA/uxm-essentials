package com.uxplima.uxmessentials.api.view;

/** A game mode, named without a Bukkit type so the value travels anywhere the API does. */
public enum UxmGameMode {

    /** Survival, id 0. */
    SURVIVAL,

    /** Creative, id 1. */
    CREATIVE,

    /** Adventure, id 2. */
    ADVENTURE,

    /** Spectator, id 3. */
    SPECTATOR
}
