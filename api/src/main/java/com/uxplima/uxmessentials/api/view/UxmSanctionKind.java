package com.uxplima.uxmessentials.api.view;

/** Which kind of punishment a sanction is. */
public enum UxmSanctionKind {

    /** The player cannot join the server. */
    BAN,

    /** The player cannot speak. */
    MUTE,

    /** The player is confined to a jail. */
    JAIL
}
