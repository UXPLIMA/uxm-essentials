command {
  name = "badorder"
  description = "A required argument after an optional one"
}

arguments = [
  { name = "first", type = string, optional = true }
  { name = "second", type = string }
]

actions = ["message:<green>never reached."]
