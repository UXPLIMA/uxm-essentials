# Homes — /home /sethome /delhome.
enabled = true
default-limit = 3                   # homes per player without a uxmessentials.home.limit.<n> node
list-display = "gui"               # how bare /homes presents the list: "gui" opens the browse menu, "chat" prints the chat list
