# Communication — info pages. Operator content; rendered as MiniMessage, never translated.
#
# Each entry auto-registers a command of the same name (/rules, /motd, /info, …) that prints the lines to the
# runner. The command is gated by uxmessentials.communication.info.<name>. Add or remove pages freely; an empty
# info-pages block registers no info commands, so this file is inert by default.
info-pages {
  # rules = [
  #   "<gold><bold>Server rules</bold></gold>"
  #   "<gray>1. Be respectful.</gray>"
  #   "<gray>2. No griefing.</gray>"
  # ]
  # motd = [
  #   "<green>Welcome, {player}!</green>"
  # ]
}
