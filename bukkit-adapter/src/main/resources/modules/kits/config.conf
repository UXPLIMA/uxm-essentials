# Kits — /kit /kits. (Per-kit definitions live one file per kit under kits/.)
enabled = true
list-display = "gui"               # how bare /kits presents the list: "gui" opens the browse menu, "chat" prints the chat list
