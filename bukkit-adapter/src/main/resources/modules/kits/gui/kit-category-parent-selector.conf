# The parent-category selector, opened from a category's settings to nest that category under another one. It lists
# every category except the one being edited, since a category can never be its own parent, plus a button that clears
# the parent and a back button to the category's settings.
#
# The grid is the kits:category-parent-selector list source, and each candidate's icon, name and lore come from the
# category definition itself through the kit_parent_* placeholders. The lore placeholder expands to the candidate's
# own lore lines, then its id, then the click hint. Fixed labels resolve from the kits message catalog.
#
# The geometry mirrors the original selector: the grid over slots 0..44, page arrows at 45/46, clear at 49, back at 53.
title = "@kit.editor.category.parent-selector.title"
rows = 6
open-actions = ["sound:ITEM_BOOK_PAGE_TURN 0.7 1.2"]

items {
  # The grey-glass backdrop. Lowest priority, so the grid and the buttons draw on top of it.
  filler {
    slots    = ["0-53"]
    material = GRAY_STAINED_GLASS_PANE
    name     = ""
    priority = 0
  }

  # One icon per candidate category. A left click nests the edited category under it and reopens its settings.
  candidates {
    slots    = ["0-44"]
    priority = 5
    list {
      source = "kits:category-parent-selector"
      template {
        material = "%kit_parent_icon%"
        name     = "%kit_parent_name%"
        lore     = ["%kit_parent_lore%"]
        click { left = ["kits:category-parent-assign"] }
      }
    }
  }

  # Previous page of categories.
  previous {
    slot     = 45
    type     = PREVIOUS
    material = ARROW
    name     = "@kit.menu.prev"
    priority = 10
  }

  # Next page of categories.
  next {
    slot     = 46
    type     = NEXT
    material = ARROW
    name     = "@kit.menu.next"
    priority = 10
  }

  # Clear the parent, so the edited category sits at the top level again.
  none {
    slot     = 49
    material = BARRIER
    name     = "@kit.editor.category.selector.none.name"
    lore     = ["@kit.editor.category.selector.none.lore"]
    priority = 10
    click { left = ["kits:category-parent-clear"] }
  }

  # Back to the category's settings, changing nothing.
  back {
    slot     = 53
    material = ARROW
    name     = "@kit.editor.settings.back-button"
    priority = 10
    click { left = ["kits:category-parent-back"] }
  }
}
