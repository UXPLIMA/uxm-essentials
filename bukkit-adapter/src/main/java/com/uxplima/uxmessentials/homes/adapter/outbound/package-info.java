/**
 * The homes context's outbound Bukkit adapter: {@code TeleportHomeAdapter}, the {@code HomeTeleporter}
 * implementation that delegates a home teleport to the teleport context's gated engine, so the homes
 * context never re-implements movement.
 */
@org.jspecify.annotations.NullMarked
package com.uxplima.uxmessentials.homes.adapter.outbound;
