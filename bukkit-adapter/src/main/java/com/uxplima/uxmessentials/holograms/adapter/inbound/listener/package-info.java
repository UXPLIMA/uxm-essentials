/**
 * The holograms context's inbound listeners: the visibility listener that re-evaluates permission-gated
 * holograms for a player when they join so they see the ones they qualify for at once.
 */
@NullMarked
package com.uxplima.uxmessentials.holograms.adapter.inbound.listener;

import org.jspecify.annotations.NullMarked;
