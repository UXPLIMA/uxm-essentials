/**
 * The vote context's inbound GUI views. {@link VoteSitesGuiView} renders the configured vote sites
 * in a paginated chest inventory with per-site cooldown status; a click on a ready site sends the
 * player an Adventure {@code openUrl} component so their client can open the link.
 */
@org.jspecify.annotations.NullMarked
package com.uxplima.uxmessentials.vote.adapter.inbound.gui;
