/**
 * The moderation context's inbound listeners: the ban-on-login enforcement at {@code PlayerLoginEvent}
 * HIGHEST, the jail/seen lifecycle on join/quit (the online-only countdown's connection edges), and the
 * freeze move-cancel.
 */
@org.jspecify.annotations.NullMarked
package com.uxplima.uxmessentials.moderation.adapter.inbound.listener;
