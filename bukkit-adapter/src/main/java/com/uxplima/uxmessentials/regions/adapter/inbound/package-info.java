/**
 * The regions context's inbound adapters: the {@code /regions} Brigadier command and the menu-engine-backed region
 * list GUI. They translate a player's command into a {@code RegionService} read and render the result through the
 * shared menu engine; no raw inventory is created here.
 */
@NullMarked
package com.uxplima.uxmessentials.regions.adapter.inbound;

import org.jspecify.annotations.NullMarked;
