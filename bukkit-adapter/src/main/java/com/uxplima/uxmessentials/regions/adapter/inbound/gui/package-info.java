/**
 * The regions context's inbound GUI adapter: the {@code RegionListView} paginated region browser (built on the
 * shared {@link com.uxplima.uxmessentials.shared.adapter.inbound.gui.EntityListView}, so it renders through the menu
 * engine) and the {@code RegionRow} presentation snapshot it paints from.
 */
@NullMarked
package com.uxplima.uxmessentials.regions.adapter.inbound.gui;

import org.jspecify.annotations.NullMarked;
