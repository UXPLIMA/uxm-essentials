/**
 * The regions context's inbound command adapter: {@code /regions [world]}, gated on
 * {@code uxmessentials.regions.list}, which opens the region-list GUI (or degrades to a "WorldGuard not installed"
 * reply when the soft dependency is absent).
 */
@NullMarked
package com.uxplima.uxmessentials.regions.adapter.inbound.command;

import org.jspecify.annotations.NullMarked;
