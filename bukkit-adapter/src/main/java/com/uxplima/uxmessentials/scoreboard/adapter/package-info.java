/**
 * The scoreboard context's adapter root: the operator-content holder
 * ({@link com.uxplima.uxmessentials.scoreboard.adapter.ScoreboardSettings}) and its HOCON codec, plus the wiring
 * ({@link com.uxplima.uxmessentials.scoreboard.adapter.ScoreboardWiring}) that constructs the renderer, the render
 * timer, the PDC visibility store, the connection listener, and the {@code /scoreboard} command over the kernel ports.
 */
@NullMarked
package com.uxplima.uxmessentials.scoreboard.adapter;

import org.jspecify.annotations.NullMarked;
