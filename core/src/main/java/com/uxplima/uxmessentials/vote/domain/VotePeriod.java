package com.uxplima.uxmessentials.vote.domain;

/** The time granularity over which a vote total is accumulated and optionally reset. */
public enum VotePeriod {
    DAILY,
    WEEKLY,
    MONTHLY,
    ALLTIME
}
