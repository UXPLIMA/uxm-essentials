/**
 * Application layer of the customcommands context: the use case that walks an operator-defined command through its
 * gates, the outbound ports it reaches the rest of the server through, the typed view of the module's config, and
 * the message keys every line it shows resolves against. No Bukkit, Paper, Kyori, or logging type appears here.
 */
@org.jspecify.annotations.NullMarked
package com.uxplima.uxmessentials.customcommands.application;
