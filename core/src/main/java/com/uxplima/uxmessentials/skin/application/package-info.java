/**
 * The skin context's application layer: the {@link com.uxplima.uxmessentials.skin.application.SkinModule} feature
 * module identity, and (as the context fills out) the typed config, the message catalog and the use cases that
 * resolve, store and apply a skin through the context's ports. Orchestration only: the domain holds the rules and
 * the ports reach the outside. Pure Java: no Bukkit, Paper, Kyori, or SLF4J.
 */
@NullMarked
package com.uxplima.uxmessentials.skin.application;

import org.jspecify.annotations.NullMarked;
