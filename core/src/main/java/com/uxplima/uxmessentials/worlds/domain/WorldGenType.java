package com.uxplima.uxmessentials.worlds.domain;

/** The vanilla world-generation preset (maps to Bukkit {@code WorldType} in the adapter). */
public enum WorldGenType {
    NORMAL,
    FLAT,
    AMPLIFIED,
    LARGE_BIOMES
}
