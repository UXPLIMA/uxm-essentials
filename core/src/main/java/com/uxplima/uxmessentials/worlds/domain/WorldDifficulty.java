package com.uxplima.uxmessentials.worlds.domain;

/** Per-world difficulty (maps to Bukkit {@code Difficulty} in the adapter). */
public enum WorldDifficulty {
    PEACEFUL,
    EASY,
    NORMAL,
    HARD
}
